"""Local environment configuration."""

config = {
    "externalEndpoints": {
        "projections_consumer_api": {
            "baseUrl": "https://projections-consumer-api.onrender.com",
            "timeout": 10000,
        }
    }
}

